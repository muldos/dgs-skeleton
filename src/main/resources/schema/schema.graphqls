type Query {
  shows(titleFilter: String): [Show]
  diagnosis: TechDetail
}

type Mutation {
  addShow(input: ShowInput): Show
}

input ShowInput {
  title: String
  releaseYear: Int
}
type Show {
  title: String
  releaseYear: Int
}

type TechDetail {
  hostname: String
  authEndpoint: String
  podIp: String
  podName: String
}
